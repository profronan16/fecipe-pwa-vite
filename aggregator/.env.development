# Caminho local do service account (Windows exemplo)
GOOGLE_APPLICATION_CREDENTIALS="C:\firebase\serviceAccount.json"

# Firestore (opcional; worker usa @google-cloud/firestore com credencial acima)
FIREBASE_PROJECT_ID=fecipeifpr-facce

# Porta local do aggregator
PORT=8787

# Origens que podem chamar a API local
AGG_ALLOWED_ORIGINS=http://localhost:5173

# Token para proteger /recompute
AGGREGATOR_TOKEN="16061993Rr@"

# (opcional) usado em UI para montar URL
APP_ORIGIN=http://localhost:8787
